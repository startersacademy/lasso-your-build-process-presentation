{
  "name": "lasso-your-build-process-presentation",
  "version": "1.0.0",
  "description": "HTML 5 presentation about how to use lasso.js to replace various parts of a typical front end workflow.",
  "config": {
    "source_dir": "./source",
    "build_dir": "./build",
    "pdf_path": "./build/slides.pdf"
  },
  "scripts": {
    "init": "prez $npm_package_config_source_dir $npm_package_config_build_dir --init",
    "present": "prez $npm_package_config_source_dir $npm_package_config_build_dir --serve",
    "dev": "prez $npm_package_config_source_dir $npm_package_config_build_dir --serve --watch $npm_package_config_pdf_path",
    "watch": "prez $npm_package_config_source_dir $npm_package_config_build_dir --watch",
    "print": "prez $npm_package_config_source_dir $npm_package_config_build_dir --print $npm_package_config_pdf_path"
  },
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/startersacademy/presentation-boilerplate.git"
  },
  "keywords": [
    "presentation",
    "meetup"
  ],
  "author": "Todd Bashor",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/startersacademy/presentation-boilerplate/issues"
  },
  "homepage": "https://github.com/startersacademy/presentation-boilerplate#readme",
  "dependencies": {
    "prez": "^4.4.0"
  }
}
